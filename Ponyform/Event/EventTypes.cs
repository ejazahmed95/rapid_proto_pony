namespace Ponyform.Event {
    /**
     * Enum class with the list of ALL in game events
     */
    public enum GameEvent {
        ActivitySet,
        ActivityCleared,
		StartedEating,
		StoppedEating,
		StartedGrooming,
		StoppedGrooming
	}
}